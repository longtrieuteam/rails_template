# Rails Template

## How to generate project

Just run command:
```bash
APP_NAME=rails_template sh -c "$(curl -fsSL https://raw.githubusercontent.com/longtrieuteam/rails_template/master/install.sh)"
```

with `APP_NAME` is the name of project. Default is `rails_template`
